package brandon.dl;

import brandon.dl.fx.DriverLicenseTableView;
import javafx.application.Application;

public class DriverLicenseApplication {
    public static void main(String[] args) {
        Application.launch(DriverLicenseTableView.class, args);
    }
}